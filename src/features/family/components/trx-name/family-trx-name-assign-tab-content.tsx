'use client'

import { useQueryString } from "@/lib/query-string"

export const FamilyTrxNameAssignTabContent = () => {
  const url = useQueryString()
  return (
    <div>FamilyTrxNameAssignTabContent</div>
  )
}
